@model IEnumerable<testforproject.Models.Event>

@{
    ViewData["Title"] = "Dashboard";
}

<link rel="stylesheet" href="~/css/dashboard.css">
<div class="dashboard-body">
    <header class="top-bar">
        <div style="font-size: 24px;">☰</div>
        <div class="search-container">
            <input type="text" placeholder="Search activities...">
        </div>
        <div class="auth-buttons">
            <a href="@Url.Action("Create", "CreateActivity")" style="background: #ff4d4d; padding: 10px 20px; border-radius: 20px;">+ Create Activity</a>
            <a href="#">Login</a>
        </div>
    </header>

    <section class="category-row">
        <div class="cat-card">Category 1</div>
        <div class="cat-card active">Category 2</div>
        <div class="cat-card">Category 3</div>
    </section>

    <nav class="filter-nav">
        <button class="filter-btn active">Recommended</button>
        <button class="filter-btn">Participant</button>
        <button class="filter-btn">Recent</button>
    </nav>

    <section class="event-grid" id="eventGrid">
        @if (Model != null)
        {
            @foreach (var item in Model)
            {
                
                <div class="event-card" id="event-card-@item.Eid">

                    <div class="card-img">Image Placeholder</div>
                    <div class="card-content">
                        <h4>@item.Name</h4>
                        <p>👤 Owner: @item.Owner</p>
                        <p>📍 Location: @item.Location</p>
                        <p>Max Participant: @item.MaxParticitpant</p>
                        <p>ExpiredDate: @item.ExpiredDate<p>
                        <p>Open: @item.EventStart </p>
                        <p>Close: @item.EventStop</p>
                        <p>Status: @item.status</p>

                        <div class="card-footer">
                            <span style="font-size: 13px;">🎯 Target: @item.MaxParticitpant</span>
                            <div style="display: flex; gap: 8px; align-items: center;">
                                <button class="join-btn">Join</button>

                                <button type="button"
                                        onclick="deleteAjax(@item.Eid)"
                                        style="background: none; color: #ff4d4d; border: 1px solid #ff4d4d; padding: 4px 10px; border-radius: 20px; cursor: pointer; font-size: 11px;">
                                    Delete
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            }
        }
    </section>
    <div class="text-center mt-5 mb-5" id="viewMoreContainer">
        <button class="btn-view-more" onclick="loadMore()">View More</button>
    </div>
    <script src="~/js/dashboard/loadmore.js"></script>
</div>

<script>
    async function deleteAjax(id) {
        if (!confirm('Are you sure you want to delete this activity?')) return;

        try {
           
            const response = await fetch('/Home/DeleteActivity/' + id, {
                method: 'POST'
            });

            
            const result = await response.json();

            if (result.success) {
                
                const card = document.getElementById('event-card-' + id);
                if (card) {
                   
                    card.style.transition = "all 0.5s";
                    card.style.opacity = "0";
                    card.style.transform = "scale(0.9)";
                    setTimeout(() => card.remove(), 500);
                }
            } else {
                alert('เกิดข้อผิดพลาด: ' + result.message);
            }
        } catch (error) {
            console.error('Error:', error);
            alert('ไม่สามารถเชื่อมต่อกับ Server ได้');
        }
    }
</script>